@model IEnumerable<ASPMVCdemo.Models.Employee>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.E_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.E_code)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.E_salary)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.E_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.E_code)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.E_salary)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.E_id }) |
                @Html.ActionLink("Details", "Details", new { id=item.E_id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.E_id })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
